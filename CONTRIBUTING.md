# 开发与审查流程

本仓库对应 `esp32s3-differential-drive` 路线。`main` 的首个提交仅建立
仓库规范，01/02/03 固件由后续功能分支通过 PR 引入。

## 分支与提交

- `main` 用于已审查的基线；新功能使用 `feat/`，修复使用 `fix/`。
- 提交消息采用 `feat:`、`fix:`、`test:`、`docs:`、`chore:` 等前缀。
- PR 面向实际功能范围；说明输入、输出、异常行为以及兼容性影响。
- 提交源文件、配置默认值、依赖锁定信息和测试；本地网络配置与构建产物留在本机。
- micro-ROS 使用固定提交的子模块，兼容性修改以独立补丁保存。

## Validation

运行 `tools/test-host-core.ps1`，并按改动范围执行 ESP-IDF 编译。
修改核心逻辑后补充有意义的边界或故障测试。PR 的自动检查必须明确其覆盖范围。

请明确区分软件检查、接口模块检查、开发板检查和整车检查。编译只能证明代码能构建；主机测试不能证明外设、ROS 2 话题、传感器或整车已工作。历史检查结果不能直接归属到新提交。

## PR 交付

PR 应包含目的与行为、改动范围、复现命令、实际结果、验证范围和剩余工作。
CI 通过之后仍需审查实现与证据；PR 创建和 CI 通过均不等于合并。
保持 PR 待审，获得明确合并指令后再合并。需要撤回已合并代码时使用
独立 revert 提交，保留审查与验证历史。

学习验收与代码合并分别记录。02 的正式测试题和综合题未完成时，不得
把软件测试通过写成学习模块正式通过。
